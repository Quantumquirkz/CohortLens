# CohortLens — Environment Variables
# Copy to .env and configure before running the app.
# Each app also has its own .env.example:
#   apps/api-ts/.env.example
#   apps/web/.env (optional — mock data used if absent)

# ── Database (Neon PostgreSQL) ────────────────────────────────────────────────
# Get a free database at https://neon.tech
NEON_DATABASE_URL=postgresql://user:pass@host/dbname?sslmode=require

# ── Auth ──────────────────────────────────────────────────────────────────────
JWT_SECRET=change-me-in-production-min-32-chars
DEFAULT_AUTH_USER=admin
DEFAULT_USER_PASSWORD=admin

# ── API Server ────────────────────────────────────────────────────────────────
PORT=8001
NODE_ENV=development

# ── Web App ───────────────────────────────────────────────────────────────────
# Used by apps/web (Express + React)
DATABASE_URL=postgresql://user:pass@host/dbname?sslmode=require
WEB_PORT=5000

# ── AI / LLM (optional) ───────────────────────────────────────────────────────
# Get a free key at https://console.groq.com
# Without this key, recommendations fall back to rule-based logic.
GROQ_API_KEY=gsk_...

# ── Feature Flags ─────────────────────────────────────────────────────────────
# All flags default to false. Manage via Admin API in production.
FEATURE_FLAG_V2_ENABLED=true
FEATURE_FLAG_V2_PRIMARY=true
FEATURE_FLAG_V1_DEPRECATED=true
FEATURE_FLAG_SHADOW_MODE=false
FEATURE_FLAG_MIGRATION_LOGGING=false
