# Aave v3 — subgraphs per network

Each network has its own manifest and data sources:

- **[polygon/](polygon/)** — primary deployment (Polygon / Amoy per `subgraph.yaml`).
- **[ethereum/](ethereum/)** — template for Ethereum mainnet (fill in contract addresses).

Build and deploy from the subdirectory for the target network.
