# @cohortlens/contracts

Shared API contracts and typed API client for CohortLens v2.

## Includes

- Zod request/response schemas.
- TypeScript inferred types.
- `CohortLensApiClient` for `/api/v2/*`.
