name: CI (Deprecated - Python v1)

# This workflow is DEPRECATED.
# The Python backend (apps/api/) and legacy frontend (frontend/) have been
# archived. All active CI is now in ci-ts.yml (NestJS API + React web app).
#
# This file is kept for historical reference only and will not run any jobs.

on:
  workflow_dispatch:

jobs:
  deprecated-notice:
    runs-on: ubuntu-latest
    steps:
      - name: Python v1 CI is deprecated
        run: |
          echo "This workflow is deprecated."
          echo "Python backend (apps/api/) has been archived in branch archive/python-v1."
          echo "Legacy Next.js frontend (frontend/) has been archived in branch archive/nextjs-frontend."
          echo "Active CI is in .github/workflows/ci-ts.yml"
